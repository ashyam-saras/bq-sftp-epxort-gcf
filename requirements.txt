# Core
flask>=2.0.0
gunicorn>=21.0.0

# Google Cloud
google-cloud-storage>=2.0.0

# SFTP
# Pinned: paramiko 5.0.0 dropped legacy host-key algorithms (ssh-rsa/ssh-dss)
# that sftp.boxouthealth.com still requires. 4.0.0 negotiates fine. Do not bump
# without testing against the SFTP server. See incident 2026-06-05.
paramiko==4.0.0
tenacity>=8.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
